def merge_value(number1, number2):
		variable = len(number1) + len(number2)
		return 	variable

def get_sum(number):
	sum_of_number = 0
	for count in range(len(number)):
		sum_of_number += number[count]
		
	sum_of_variable = sum_of_number * len(number) - sum_of_number
	return sum_of_variable	 

def get_palindrom(variable):
	value1 = 0
	value2 = 0

	for counter in range(1,value1, 1):
		for index in range(1, value2, -1):
			if counter == index:
				return true
			else:
				return false

def get_average(number):
	average = 0
	for count in range(len(number)):
		avarage = sum(count)/number
		return average

def get_reverse_string(string):
	string = slice(hello)
	return string

def get_sum(number):
	sum_of_number = 0;
	for number in range(1,5):		
		if number % 2 ==0:
			sum_of_number += number
	return sum_of_number
	
	