def get_sum(number):
	return 0

def get_sum(number):
	sum_of_number = 0;
	for number in range(1,5):		
		if number % 2 ==0:
			sum_of_number += number
	return sum_of_number
"""""
def get_vowel(word:str):
	vowel = "a,e,i,o,u"
	letter = 0
	for vowel in vowel:
		for character in word:
			if character == vowel:
				letter += 1	
		return letter 


def get_sum(number):
	number = 0;
	for count in range(len(number)):
		sum_of_number += (count + number)
		return 	sum_of_number
"""""
def merge_value(number1,number2):
		variable = len(number1)+len(number2)
		return 	variable

